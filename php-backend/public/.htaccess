RewriteEngine On

# Block sensitive files
RewriteRule \.(env|git|log|sql|bak|md|yml|yaml|json)$ - [F,L]
RewriteRule ^\. - [F,L]

# Existing rewrites
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.php [QSA,L]
